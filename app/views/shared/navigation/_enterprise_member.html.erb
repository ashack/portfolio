<nav class="flex flex-1 flex-col" aria-label="Sidebar">
  <ul role="list" class="flex flex-1 flex-col gap-y-7">
    <!-- Main Navigation -->
    <li>
      <ul role="list" class="-mx-2 space-y-1">
        <li>
          <%= link_to enterprise_dashboard_path(enterprise_slug: @enterprise_group.slug), class: "group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 hover:bg-purple-50 hover:text-purple-600 #{current_page?(enterprise_dashboard_path(enterprise_slug: @enterprise_group.slug)) ? 'bg-purple-50 text-purple-600' : 'text-gray-700'}" do %>
            <%= icon "buildings", class: "size-6 shrink-0 #{current_page?(enterprise_dashboard_path(enterprise_slug: @enterprise_group.slug)) ? 'text-purple-600' : 'text-gray-400 group-hover:text-purple-600'}" %>
            Dashboard
          <% end %>
        </li>
      </ul>
    </li>

    <!-- Administration Section (if user is enterprise admin) -->
    <% if current_user.enterprise_admin?(@enterprise_group) %>
      <li>
        <div class="text-xs font-semibold leading-6 text-gray-400">Administration</div>
        <ul role="list" class="-mx-2 mt-2 space-y-1">
          <li>
            <%= link_to enterprise_members_path(enterprise_slug: @enterprise_group.slug), class: "group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 hover:bg-purple-50 hover:text-purple-600 #{current_page?(enterprise_members_path(enterprise_slug: @enterprise_group.slug)) ? 'bg-purple-50 text-purple-600' : 'text-gray-700'}" do %>
              <%= icon "users", class: "size-6 shrink-0 #{current_page?(enterprise_members_path(enterprise_slug: @enterprise_group.slug)) ? 'text-purple-600' : 'text-gray-400 group-hover:text-purple-600'}" %>
              Members
            <% end %>
          </li>
          <li>
            <%= link_to enterprise_billing_path(enterprise_slug: @enterprise_group.slug), class: "group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 hover:bg-purple-50 hover:text-purple-600 #{current_page?(enterprise_billing_path(enterprise_slug: @enterprise_group.slug)) ? 'bg-purple-50 text-purple-600' : 'text-gray-700'}" do %>
              <%= icon "credit-card", class: "size-6 shrink-0 #{current_page?(enterprise_billing_path(enterprise_slug: @enterprise_group.slug)) ? 'text-purple-600' : 'text-gray-400 group-hover:text-purple-600'}" %>
              Billing
            <% end %>
          </li>
          <li>
            <%= link_to enterprise_settings_path(enterprise_slug: @enterprise_group.slug), class: "group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 hover:bg-purple-50 hover:text-purple-600 #{current_page?(enterprise_settings_path(enterprise_slug: @enterprise_group.slug)) ? 'bg-purple-50 text-purple-600' : 'text-gray-700'}" do %>
              <%= icon "gear", class: "size-6 shrink-0 #{current_page?(enterprise_settings_path(enterprise_slug: @enterprise_group.slug)) ? 'text-purple-600' : 'text-gray-400 group-hover:text-purple-600'}" %>
              Settings
            <% end %>
          </li>
          <li>
            <%= link_to enterprise_notification_categories_path(enterprise_slug: @enterprise_group.slug), class: "group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 hover:bg-purple-50 hover:text-purple-600 #{current_page?(enterprise_notification_categories_path(enterprise_slug: @enterprise_group.slug)) ? 'bg-purple-50 text-purple-600' : 'text-gray-700'}" do %>
              <%= icon "tag", class: "size-6 shrink-0 #{current_page?(enterprise_notification_categories_path(enterprise_slug: @enterprise_group.slug)) ? 'text-purple-600' : 'text-gray-400 group-hover:text-purple-600'}" %>
              Notification Categories
            <% end %>
          </li>
        </ul>
      </li>
    <% end %>
  </ul>
</nav>