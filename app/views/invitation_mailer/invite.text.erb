You're invited!

<% if @invitation.enterprise_invitation? %>
Hi there,

<%= @invited_by.email %> has invited you to join <%= @organization.name %> as <%= @invitation.role == 'admin' ? 'an administrator' : 'a member' %>.

Click the link below to accept the invitation and create your account:

<%= @accept_url %>
<% else %>
Hi there,

<%= @invited_by.email %> has invited you to join <%= @team.name %> as <%= @invitation.role == 'admin' ? 'an administrator' : 'a member' %>.

Click the link below to accept the invitation and create your account:

<%= @accept_url %>
<% end %>

This invitation will expire in 7 days.

If you weren't expecting this invitation, you can safely ignore this email.

Best regards,
The SaaS App Team
