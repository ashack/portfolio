<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dashboard - SaaS App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Flash Messages -->
    <% if flash[:notice].present? %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= flash[:notice] %></span>
      </div>
    <% end %>
    
    <% if flash[:alert].present? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= flash[:alert] %></span>
      </div>
    <% end %>

    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="bg-gray-800 text-white w-64 space-y-6 py-7 px-2 absolute inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition duration-200 ease-in-out" id="sidebar">
        <!-- User Info Header -->
        <div class="text-white flex items-center space-x-2 px-4">
          <div class="flex-shrink-0">
            <% if current_user.avatar.attached? %>
              <%= image_tag current_user.avatar.variant(resize_to_fill: [40, 40]), 
                  class: "h-10 w-10 rounded-full", 
                  alt: current_user.full_name %>
            <% else %>
              <div class="h-10 w-10 bg-indigo-600 rounded-full flex items-center justify-center">
                <span class="text-sm font-bold text-white"><%= current_user.initials %></span>
              </div>
            <% end %>
          </div>
          <div>
            <span class="text-lg font-bold"><%= current_user.full_name.presence || "Dashboard" %></span>
            <p class="text-xs text-gray-400">Individual Account</p>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="mt-10">
          <%= link_to root_path, class: "#{current_page?(root_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
            <%= icon "house", class: "mr-4 h-6 w-6" %>
            Dashboard
          <% end %>

          <%= link_to users_profile_path(current_user), class: "#{request.path.include?('/profile') && !request.path.include?('/edit') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
            <%= icon "user", class: "mr-4 h-6 w-6" %>
            My Profile
            <% if current_user.profile_completion_percentage < 100 %>
              <span class="ml-auto inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-600 text-yellow-100">
                <%= current_user.profile_completion_percentage %>%
              </span>
            <% end %>
          <% end %>

          <% if current_user.respond_to?(:payment_processor) && current_user.payment_processor.present? %>
            <%= link_to users_billing_index_path, class: "#{request.path.include?('/billing') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
              <%= icon "credit-card", class: "mr-4 h-6 w-6" %>
              Billing
            <% end %>
          <% end %>

          <%= link_to users_subscription_path, class: "#{request.path.include?('/subscription') ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
            <%= icon "currency-dollar", class: "mr-4 h-6 w-6" %>
            Subscription
          <% end %>

          <%= link_to users_settings_path, class: "#{current_page?(users_settings_path) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
            <%= icon "gear", class: "mr-4 h-6 w-6" %>
            Settings
          <% end %>

          <div class="mt-6">
            <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider">Support</p>
            <div class="mt-2 space-y-1">
              <a href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium">
                <%= icon "book-open", class: "inline-block mr-2 h-4 w-4" %>
                Documentation
              </a>
              <a href="#" class="text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" data-controller="help-widget" data-action="click->help-widget#show">
                <%= icon "lifebuoy", class: "inline-block mr-2 h-4 w-4" %>
                Help Center
              </a>
            </div>
          </div>

          <!-- Create Team Option -->
          <% if current_user.direct? && !current_user.owns_team? %>
            <div class="mt-6 px-3">
              <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">Upgrade</p>
              <%= link_to pricing_path, class: "block w-full text-center px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-md text-sm font-medium transition-colors" do %>
                <%= icon "rocket-launch", class: "inline-block mr-1 h-4 w-4" %>
                Upgrade to Team
              <% end %>
            </div>
          <% end %>
        </nav>

        <!-- User Menu (Bottom) -->
        <div class="absolute bottom-0 left-0 right-0 p-4 bg-gray-700">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <% if current_user.avatar.attached? %>
                <%= image_tag current_user.avatar.variant(resize_to_fill: [32, 32]), 
                    class: "h-8 w-8 rounded-full", 
                    alt: current_user.full_name %>
              <% else %>
                <div class="w-8 h-8 bg-gray-500 rounded-full flex items-center justify-center">
                  <span class="text-sm font-medium text-white"><%= current_user.initials %></span>
                </div>
              <% end %>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-white truncate"><%= current_user.email %></p>
              <p class="text-xs text-gray-400">
                <%= current_user.plan&.name || "Free Plan" %>
              </p>
            </div>
          </div>
          <div class="mt-3 flex items-center justify-between">
            <%= link_to "My Profile", users_profile_path(current_user), class: "text-gray-400 hover:text-white text-sm" %>
            <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-gray-400 hover:text-white text-sm" %>
          </div>
        </div>
      </div>

      <!-- Main Content -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Mobile menu button -->
        <div class="md:hidden bg-gray-800 text-white p-4 flex items-center justify-between" data-controller="sidebar">
          <h1 class="text-xl font-bold">SaaS App</h1>
          <button data-action="click->sidebar#toggle" class="p-2 rounded-md hover:bg-gray-700">
            <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>
        </div>

        <!-- Top bar -->
        <header class="bg-white shadow-sm border-b border-gray-200">
          <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between">
              <h1 class="text-2xl font-bold text-gray-900">
                <%= content_for?(:page_title) ? yield(:page_title) : "Dashboard" %>
              </h1>
              <div class="flex items-center space-x-4">
                <!-- Notification Center -->
                <%= render "shared/notification_center" %>
                
                <!-- Quick Actions -->
                <div class="hidden lg:flex items-center space-x-2">
                  <%= yield :header_actions %>
                </div>
              </div>
            </div>
          </div>
        </header>

        <!-- Page Content -->
        <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-50">
          <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
            <%= yield %>
          </div>
        </main>
      </div>
    </div>

    <!-- Help Widget -->
    <%= render "shared/help_widget" %>
  </body>
</html>