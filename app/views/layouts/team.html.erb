<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Team Dashboard - SaaS App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Flash Messages -->
    <% if flash[:notice].present? %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= flash[:notice] %></span>
      </div>
    <% end %>
    
    <% if flash[:alert].present? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 mx-4 mt-4" role="alert">
        <span class="block sm:inline"><%= flash[:alert] %></span>
      </div>
    <% end %>

    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="bg-gray-800 text-white w-64 space-y-6 py-7 px-2 absolute inset-y-0 left-0 transform -translate-x-full md:relative md:translate-x-0 transition duration-200 ease-in-out">
        <!-- Team Info -->
        <div class="text-white flex items-center space-x-2 px-4">
          <div class="flex-shrink-0 h-8 w-8 bg-indigo-600 rounded-lg flex items-center justify-center">
            <span class="text-sm font-bold text-white"><%= @team.name.first.upcase %></span>
          </div>
          <div>
            <span class="text-lg font-bold"><%= @team.name %></span>
            <p class="text-xs text-gray-400">Team Dashboard</p>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="mt-10">
          <%= link_to team_root_path(team_slug: @team.slug), class: "#{request.path == team_root_path(team_slug: @team.slug) ? 'bg-gray-900 text-white' : 'text-gray-300 hover:bg-gray-700 hover:text-white'} group flex items-center px-2 py-2 text-base font-medium rounded-md mb-1" do %>
            <%= icon "house", class: "mr-4 h-6 w-6" %>
            Dashboard
          <% end %>

          <% if current_user.team_admin? %>
            <div class="mt-4">
              <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider">Team Admin</p>
              <div class="mt-2 space-y-1">
                <%= link_to "Admin Dashboard", team_admin_root_path, class: "#{request.path == team_admin_root_path ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Members", team_admin_members_path, class: "#{request.path.include?('/members') ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Invitations", team_admin_invitations_path, class: "#{request.path.include?('/invitations') ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Email Requests", team_admin_email_change_requests_path, class: "#{request.path.include?('/email_change_requests') ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Billing", team_admin_billing_index_path, class: "#{request.path.include?('/billing') ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Settings", team_admin_settings_path, class: "#{request.path.include?('/settings') ? 'bg-gray-700' : ''} text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            </div>
          <% end %>

          <div class="mt-4">
            <p class="px-3 text-xs font-semibold text-gray-400 uppercase tracking-wider">Team Features</p>
            <div class="mt-2 space-y-1">
              <%= link_to "Collaboration", "#", class: "text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Projects", "#", class: "text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Documents", "#", class: "text-gray-300 hover:bg-gray-700 hover:text-white block px-3 py-2 rounded-md text-sm font-medium" %>
            </div>
          </div>
        </nav>

        <!-- User Menu -->
        <div class="absolute bottom-0 left-0 right-0 p-4 bg-gray-700">
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <div class="w-8 h-8 bg-gray-500 rounded-full flex items-center justify-center">
                <span class="text-sm font-medium text-white"><%= current_user.email.first.upcase %></span>
              </div>
            </div>
            <div class="ml-3">
              <p class="text-sm font-medium text-white"><%= current_user.email %></p>
              <p class="text-xs text-gray-400">
                Team <%= current_user.team_role.capitalize %>
              </p>
            </div>
          </div>
          <div class="mt-3">
            <%= link_to "My Profile", teams_profile_path(team_slug: params[:team_slug], id: current_user), class: "text-gray-400 hover:text-white text-sm" %>
            <span class="text-gray-500">•</span>
            <%= link_to "Back to Home", root_path, class: "text-gray-400 hover:text-white text-sm" %>
            <span class="text-gray-500">•</span>
            <%= link_to "Sign Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-gray-400 hover:text-white text-sm" %>
          </div>
        </div>
      </div>

      <!-- Main Content -->
      <div class="flex-1 flex flex-col overflow-hidden">
        <!-- Top bar -->
        <header class="bg-white shadow-sm border-b border-gray-200">
          <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between">
              <div>
                <h1 class="text-2xl font-bold text-gray-900">
                  <%= content_for?(:page_title) ? yield(:page_title) : "Team Dashboard" %>
                </h1>
                <p class="text-sm text-gray-500 mt-1">
                  <%= @team.name %> • <%= @team.plan.capitalize %> Plan • <%= @team.member_count %>/<%= @team.max_members %> members
                </p>
              </div>
              <div class="flex items-center space-x-4">
                <%= yield :header_actions %>
              </div>
            </div>
          </div>
        </header>

        <!-- Page Content -->
        <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-50">
          <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
            <%= yield %>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>