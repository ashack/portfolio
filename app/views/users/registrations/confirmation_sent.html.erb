<div class="min-h-screen bg-gray-100 flex flex-col justify-center py-6 px-4 sm:px-6">
  <div class="sm:mx-auto sm:w-full sm:max-w-lg">
    <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-green-100">
      <%= icon "check", class: "h-6 w-6 text-green-600" %>
    </div>
    <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
      Registration successful!
    </h2>
    <p class="mt-2 text-center text-sm text-gray-600">
      We've sent a confirmation link to
    </p>
    <p class="mt-2 text-center text-sm font-medium text-gray-900">
      <%= @email %>
    </p>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-lg">
    <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <div class="space-y-6">
        <!-- Success message -->
        <div class="rounded-md bg-green-50 p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <%= icon "envelope", class: "h-5 w-5 text-green-400" %>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-green-800">
                Check your inbox
              </h3>
              <div class="mt-2 text-sm text-green-700">
                <p>We've sent you an email with a confirmation link. Click the link to verify your account and get started.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Instructions -->
        <div class="space-y-4">
          <h4 class="text-sm font-medium text-gray-900">What's next?</h4>
          <ol class="list-decimal list-inside space-y-2 text-sm text-gray-600">
            <li>Open your email inbox</li>
            <li>Find the email from <%= Rails.application.config.action_mailer.default_options&.dig(:from) || "us" %></li>
            <li>Click the confirmation link</li>
            <li>Sign in to access your account</li>
          </ol>
        </div>

        <!-- Sign in button -->
        <div>
          <%= link_to "Go to Sign In", new_user_session_path, 
              class: "w-full flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>

        <!-- Help text -->
        <div class="text-sm text-gray-600 text-center">
          <p>
            Didn't receive the email? Check your spam folder or
            <%= link_to "request a new one", new_user_confirmation_path, class: "font-medium text-blue-600 hover:text-blue-500" %>.
          </p>
        </div>
      </div>
    </div>
  </div>
</div>