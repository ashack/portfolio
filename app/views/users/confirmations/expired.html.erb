<div class="min-h-screen bg-gray-100 flex flex-col justify-center py-6 px-4 sm:px-6">
  <div class="sm:mx-auto sm:w-full sm:max-w-lg">
    <div class="mx-auto flex items-center justify-center h-12 w-12 rounded-full bg-yellow-100">
      <%= icon "clock", class: "h-6 w-6 text-yellow-600" %>
    </div>
    <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
      Confirmation link expired
    </h2>
    <p class="mt-2 text-center text-sm text-gray-600">
      We've sent a new confirmation link to
    </p>
    <p class="mt-2 text-center text-sm font-medium text-gray-900">
      <%= session.delete(:expired_confirmation_email) %>
    </p>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-lg">
    <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <div class="space-y-6">
        <!-- Info message -->
        <div class="rounded-md bg-yellow-50 p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <%= icon "info", class: "h-5 w-5 text-yellow-400" %>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-yellow-800">
                New link sent
              </h3>
              <div class="mt-2 text-sm text-yellow-700">
                <p>Your confirmation link has expired, but don't worry! We've sent you a new one. Please check your email and click the fresh link to verify your account.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Instructions -->
        <div class="space-y-4">
          <h4 class="text-sm font-medium text-gray-900">What happened?</h4>
          <ul class="list-disc list-inside space-y-2 text-sm text-gray-600">
            <li>Confirmation links expire after 3 days for security</li>
            <li>We've automatically sent you a new confirmation email</li>
            <li>The new link will be valid for another 3 days</li>
          </ul>
        </div>

        <!-- Sign in button -->
        <div>
          <%= link_to "Go to Sign In", new_user_session_path, 
              class: "w-full flex justify-center py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>

        <!-- Help text -->
        <div class="text-sm text-gray-600 text-center">
          <p>
            Still having trouble? 
            <%= link_to "Contact support", "#", class: "font-medium text-blue-600 hover:text-blue-500" %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>